((module std/core)
  (defs
    (def std/core/apply
      (fn (f x)
        (f (x))))
    (def std/core/o
      (fn (f g)
        (fn (x)
          (f ((g (x)))))))
    (def std/core/ignore
      (fn (x)
        std/core/types/Unit))
    (def std/core/const
      (fn (default)
        (fn (h-_wildcard-x105_6)
          default)))
    (def std/core/same-type
      (fn (x y)
        x))
    (def std/core/while
      (fn (predicate action)
        (match (predicate ())
          [std/core/types/True (letrec* ([_ (action ())])
            (std/core/while (predicate action)))]
          [std/core/types/False std/core/types/Unit])))
    (def std/core/default/cmp/==
      (fn (x y h-implicit/cmp)
        (match (h-implicit/cmp (x y))
          [std/core/types/Eq std/core/types/True]
          [_ std/core/types/False])))
    (def std/core/default/!=
      (fn (x y h-implicit/==)
        (std/core/types/not ((h-implicit/== (x y))))))
    (def std/core/default/cmp/>
      (fn (x y h-implicit/cmp)
        (std/core/order/== ((h-implicit/cmp (x y)) std/core/types/Gt))))
    (def std/core/range/fold
      (fn (start end init f)
        (match (std/core/int/> (start end))
          [std/core/types/True init]
          [std/core/types/False (letrec* ([x (f (start init))])
            (std/core/range/fold ((std/core/int/inc (start)) end x f)))])))
    (def std/core/fold
      (fn (upto init f)
        (std/core/range/fold (0 (std/core/int/dec (upto)) init f))))
    (def std/core/range/fold-while
      (fn (start end init f)
        (match (std/core/int/> (start end))
          [std/core/types/True init]
          [std/core/types/False (match (f (start init))
            [(std/core/types/Just x) (std/core/range/fold-while ((std/core/int/inc (start)) end x f))]
            [std/core/types/Nothing init])])))
    (def std/core/fold-while
      (fn (n init f)
        (std/core/range/fold-while (0 (std/core/int/dec (n)) init f))))
    (def std/core/default/cmp/<
      (fn (x y h-implicit/cmp)
        (std/core/order/== ((h-implicit/cmp (x y)) std/core/types/Lt))))
    (def std/core/default/cmp/>=
      (fn (x y h-implicit/cmp)
        (std/core/types/not ((std/core/default/cmp/< (x y h-implicit/cmp))))))
    (def std/core/default/cmp/<=
      (fn (x y h-implicit/cmp)
        (std/core/types/not ((std/core/default/cmp/> (x y h-implicit/cmp))))))
    (def std/core/range/for
      (fn (start end action)
        (letrec* ([rep (fn (i)
          (match (std/core/int/<= (i end))
            [std/core/types/True (letrec* ([_ (action (i))])
              (rep ((std/core/int/inc (i)))))]
            [std/core/types/False std/core/types/Unit]))])
          (rep (start)))))
    (def std/core/for
      (fn (n action)
        (std/core/range/for (0 (std/core/int/- (n 1)) action))))
    (def std/core/repeat
      (fn (n action)
        (std/core/for (n (fn (i)
          (action ()))))))
    (def std/core/range/for-while
      (fn (start end action)
        (letrec* ([rep (fn (i)
          (match (std/core/int/<= (i end))
            [std/core/types/True (match (action (i))
              [std/core/types/Nothing (rep ((std/core/int/inc (i))))]
              [(std/core/types/Just x) (std/core/types/Just (x))])]
            [std/core/types/False std/core/types/Nothing]))])
          (rep (start)))))
    (def std/core/for-while
      (fn (n action)
        (std/core/range/for-while (0 (std/core/int/- (n 1)) action))))
    (def std/core/main-console
      (fn (main)
        (std/core/h-extern-main-console (main))))
    (def std/core/host
      (fn ()
        (std/core/h-extern-host ())))
    (def std/core/h-default-exn
      (fn (action)
        ((fn (h-action)
          (std/core/exn/exn/h-handle ((std/core/exn/h-Hnd-exn (0 (std/core/hnd/clause-never1 ((fn (exn)
            (letrec* ([_ (std/core/console/string/print ("uncaught exception: "))])
              (std/core/console/default/show/println (exn std/core/exn/show)))))))) (fn (h-res)
            h-res) h-action))) (action))))
    (def std/core/unique-count
      (std/core/unsafe/unsafe-total ((fn ()
        (std/core/types/ref (0))))))
    (def std/core/unique
      (fn ()
        (letrec* ([ievh-1777 std/core/types/h-Hdiv] [ievh-1782 std/core/types/h-Hnodiv])
          (std/core/unsafe/unsafe-total ((fn ()
            (letrec* ([u (std/core/types/ref/! (std/core/unique-count ievh-1782))] [_ (std/core/types/set (std/core/unique-count (std/core/int/+ (u 1))))])
              u)))))))
    (def std/core/stream/head
      (fn (stream)
        (match stream
          [(std/core/Next h-x _) h-x])))
    (def std/core/stream/tail
      (fn (stream)
        (match stream
          [(std/core/Next _ h-x) h-x])))
    (def std/core/stream/h-copy
      (fn (h-this head tail)
        (letrec* ([h-uniq-headh-1867 (match head
          [(std/core/types/h-Optional h-uniq-headh-1868) h-uniq-headh-1868]
          [std/core/types/h-None (std/core/stream/head (h-this))])] [h-uniq-tailh-1878 (match tail
          [(std/core/types/h-Optional h-uniq-tailh-1879) h-uniq-tailh-1879]
          [std/core/types/h-None (std/core/stream/tail (h-this))])])
          (std/core/Next (h-uniq-headh-1867 h-uniq-tailh-1878)))))))