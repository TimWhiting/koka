(module
  std/core/vector
  (define std/core/vector/h-unsafe-vector
    (λ (n)
      (std/core/vector/h-extern-unsafe-vector n)))
  (define std/core/vector/vector-alloc
    (λ (n init)
      (std/core/vector/h-extern-vector-alloc n init)))
  (define std/core/vector/vector-alloc-total
    (λ (n f)
      (std/core/vector/h-extern-vector-alloc-total n f)))
  (define std/core/vector/h-index
    (λ (v index)
      (letrec* ([idx (std/core/int/ssize_t index)])
        (match (std/core/vector/ssize_t/< idx (std/core/vector/lengthz v))
          [std/core/types/True (std/core/vector/unsafe-idx v idx)]
          [std/core/types/False (std/core/exn/throw "index out of bounds" (std/core/types/h-Optional std/core/exn/ExnRange))]))))
  (define std/core/vector/at
    (λ (v index)
      (letrec* ([idx (std/core/int/ssize_t index)])
        (match (std/core/vector/ssize_t/< idx (std/core/vector/lengthz v))
          [std/core/types/True (std/core/types/Just (std/core/vector/unsafe-idx v idx))]
          [std/core/types/False std/core/types/Nothing]))))
  (define std/core/vector/length
    (λ (v)
      (std/core/int/ssize_t/int (std/core/vector/lengthz v))))
  (define std/core/vector/vector
    (λ (n default)
      (std/core/vector/vector-alloc (std/core/int/ssize_t n) default)))
  (define std/core/vector/vector-init-total
    (λ (n f)
      (std/core/vector/vector-alloc-total (std/core/int/ssize_t n) (λ (i)
        (f (std/core/int/ssize_t/int i))))))
  (define std/core/vector/ssize_t/decr
    (λ (i)
      (std/core/vector/ssize_t/h-extern-decr i)))
  (define std/core/vector/ssize_t/incr
    (λ (i)
      (std/core/vector/ssize_t/h-extern-incr i)))
  (define std/core/vector/forz
    (λ (n action)
      (letrec* ([rep (λ (i)
        (match (std/core/vector/ssize_t/< i n)
          [std/core/types/True (letrec* ([_ (action i)])
            (rep (std/core/vector/ssize_t/incr i)))]
          [std/core/types/False std/core/types/Unit]))])
        (rep (std/core/int/ssize_t 0)))))
  (define std/core/vector/vector-init
    (λ (n f)
      (letrec* ([len (std/core/int/ssize_t n)] [v (std/core/vector/unsafe-vector len)] [_ (std/core/vector/forz len (λ (i)
        (std/core/vector/unsafe-assign v i (f (std/core/int/ssize_t/int i)))))])
        v)))
  (define std/core/vector/foreach-indexedz
    (λ (v f)
      (std/core/vector/forz (std/core/vector/lengthz v) (λ (i)
        (f i (std/core/vector/unsafe-idx v i))))))
  (define std/core/vector/foreach
    (λ (v f)
      (std/core/vector/foreach-indexedz v (λ (h-_wildcard-x112_26 x)
        (f x)))))
  (define std/core/vector/foreach-indexed
    (λ (v f)
      (std/core/vector/foreach-indexedz v (λ (i x)
        (f (std/core/int/ssize_t/int i) x)))))
  (define std/core/vector/for-whilez
    (λ (n action)
      (letrec* ([rep (λ (i)
        (match (std/core/vector/ssize_t/< i n)
          [std/core/types/True (match (action i)
            [std/core/types/Nothing (rep (std/core/vector/ssize_t/incr i))]
            [(std/core/types/Just x) (std/core/types/Just x)])]
          [std/core/types/False std/core/types/Nothing]))])
        (rep (std/core/int/ssize_t 0)))))
  (define std/core/vector/foreach-while
    (λ (v f)
      (std/core/vector/for-whilez (std/core/vector/lengthz v) (λ (i)
        (f (std/core/vector/unsafe-idx v i))))))
  (define std/core/vector/map
    (λ (v f)
      (letrec* ([w (std/core/vector/unsafe-vector (std/core/int/ssize_t (std/core/vector/length v)))] [_ (std/core/vector/foreach-indexedz v (λ (i x)
        (std/core/vector/unsafe-assign w i (f x))))])
        w)))
  (define std/core/vector/vlist
    (λ (v tail)
      (letrec* ([h-uniq-tailh-918 (match tail
        [(std/core/types/h-Optional h-uniq-tailh-919) h-uniq-tailh-919]
        [std/core/types/h-None std/core/types/Nil])])
        (std/core/vector/h-extern-vlist v h-uniq-tailh-918))))
  (define std/core/vector/list
    (λ (v)
      (std/core/vector/vlist v std/core/types/h-None)))
  (define std/core/vector/unvlist
    (λ (xs)
      (std/core/vector/h-extern-unvlist xs)))
  (define std/core/vector/list/vector
    (λ (xs)
      (std/core/vector/unvlist xs)))
  (define std/core/vector/ssize_t/is-zero
    (λ (i)
      (std/core/vector/ssize_t/h-extern-is-zero i))))