((module std/core/unsafe)
  (defs
    (def std/core/unsafe/unsafe-total
      (fn (action)
        ((std/core/unsafe/unsafe-total-cast (action)) ())))
    (def std/core/unsafe/pretend-no-ndet
      (fn (action)
        ((std/core/unsafe/unsafe-total-cast (action)) ())))))