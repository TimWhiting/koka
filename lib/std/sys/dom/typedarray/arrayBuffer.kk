pub module std/sys/dom/typedarray/arrayBuffer

// Interface ArrayBuffer (generated by esidl 0.2.0).

import std/sys/dom/types

/* safe and unsafe conversion from all types including `any */
inline extern maybeArrayBuffer  : forall<a> (a) -> maybe<arrayBuffer>
{ inline "(#1.toString()==='[object ArrayBuffer]'?std_core.Just(#1):std_core.Nothing)" }
inline extern toArrayBuffer : forall<a> (a) -> exn arrayBuffer
{ inline "(#1.toString()==='[object ArrayBuffer]'?#1:std_core.error('toArrayBuffer: cast failed'))" }


// ArrayBuffer implements ArrayBuffer (supplemental)
inline extern byteLength : (this: arrayBuffer) -> dom uint32
{ inline "#1.byteLength" }
