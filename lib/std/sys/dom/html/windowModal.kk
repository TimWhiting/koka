pub module std/sys/dom/html/windowModal

// Interface WindowModal (generated by esidl 0.2.0).

import std/sys/dom/types

/* safe and unsafe conversion from all types including `any */
inline extern maybeWindowModal  : forall<a> (a) -> maybe<windowModal>
{ inline "(#1.toString()==='[object WindowModal]'?std_core.Just(#1):std_core.Nothing)" }
inline extern toWindowModal : forall<a> (a) -> exn windowModal
{ inline "(#1.toString()==='[object WindowModal]'?#1:std_core.error('toWindowModal: cast failed'))" }


// WindowModal implements WindowModal (supplemental)
inline extern dialogArguments : (this: windowModal) -> dom any /* anyGetter */
{ inline "#1.dialogArguments" }
inline extern returnValue : (this: windowModal) -> dom string
{ inline "#1.returnValue" }
inline extern returnValue : (this: windowModal, assigned: string) -> dom ()
{ inline "(#1.returnValue = #2)" }
