pub module sys/dom/html/canvasGradient

// Interface CanvasGradient (generated by esidl 0.2.0).

import sys/dom/types

/* safe and unsafe conversion from all types including `any */
inline extern maybeCanvasGradient  : forall<a> (a) -> maybe<canvasGradient>
{ inline "(#1.toString()==='[object CanvasGradient]'?std_core.Just(#1):std_core.Nothing)" }
inline extern toCanvasGradient : forall<a> (a) -> exn canvasGradient
{ inline "(#1.toString()==='[object CanvasGradient]'?#1:std_core.error('toCanvasGradient: cast failed'))" }


// CanvasGradient implements CanvasGradient (supplemental)
inline extern addColorStop : (this: canvasGradient, offset: float64, color: string) -> dom ()
{ inline "#1.addColorStop(#2, #3)" }

